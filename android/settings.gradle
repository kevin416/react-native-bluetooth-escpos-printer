// react-native-bluetooth-escpos-printer/android/settings.gradle

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
        maven {
            // React Native dependencies
            url "$rootDir/../node_modules/react-native/android"
        }
    }
}

rootProject.name = 'react-native-bluetooth-escpos-printer'